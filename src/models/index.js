const user = require("./user.models")
const activity = require("./activity.models")
const information = require("./information.models")
const comment = require("./comment.models")

module.exports ={
    user,
    activity ,
    information,
    comment,
}
    